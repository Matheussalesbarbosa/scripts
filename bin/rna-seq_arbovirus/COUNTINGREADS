#!/bin/bash
mkdir $HOME/datasets/rna-seq_arbovirus/counts/ # criar diretorio
featureCounts \
-a $HOME/datasets/rna-seq_arbovirus/refs/gencode.v31.chr_patch_hapl_scaff.annotation.gtf \ # arquivo de anotacao dos transcritos do genoma referencia
-C \ # nao conta fragmentos quimericos
-F GTF \ # formato do arquivo de anotacao dos transcritos do genoma referencia (GTF; SAF; GFF)
-g gene_id \ # especifica o atributo para nomear os genes (gene_id: Ensembl annotation; gene_name: gene symbol)
-o $HOME/datasets/rna-seq_arbovirus/counts/counts.txt \ # nome do arquivo de saida com as contagem dos transcritos (delimitado por tabulaco)
*bam \ # seleciona as amostras para a contagem
-p \ # os fragmentos serao contados ao inves das leituras
-s 2 \ # realiza a contagem de acordo com a forma que as leituras foram geradas (0: unstranded; 1: stranded; 2: stranded reverso)
-T 12 # numero de threads utilizados para processar simultaneamente os processos da analise
# ARQUIVOS DE SAIDA
## counts.txt: arquivo de saida com a contagem dos transcritos de todas as amostras
## counts.txt.summary: arquivo de saida com o sumario da contagem dos transcritos de todas as amostras
